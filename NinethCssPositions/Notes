Positions
-static
-relative
-absolute
- Fixed
- sticky



STATIC
1. The 1st Position to talk about is (static), by default element have (static) position by default. And they are position according to
   the normal flow of the Page, so element with static are not positioned in a special way.

2.    They not also affected by (top,right,bottom, etc properties)

3. We can change positions of the elements using margins ,floats, grid,flexbox, etc.

RELATIVE
--------
Is a position relative to it normal position